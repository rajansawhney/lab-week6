/*
Write a C Program that adds positive numbers until a 0 is typed but ignores any negative numbers typed and provides the following output:

NOTE:
1. Input should only be of type integer
2. Output should EXACTLY say "Sum = [result]" 
3. Your code MUST use continue AND break
*/
